[importlinter]
root_package = deadbolt

[contract:deterministic-engine]
name = Engine has no provider or I/O dependencies
type = forbidden
source_modules = deadbolt.engine
forbidden_modules =
    deadbolt.providers
    deadbolt.graph
    deadbolt.executor
    deadbolt.audit
    boto3
    httpx
    random
    time
    math

[contract:deterministic-plan]
name = Plan has no provider or I/O dependencies
type = forbidden
source_modules = deadbolt.plan
forbidden_modules =
    deadbolt.providers
    deadbolt.graph
    deadbolt.executor
    deadbolt.audit
    boto3
    httpx
    random
    time
    math

[contract:layers]
name = Deadbolt layers
type = layers
layers =
    deadbolt.handlers
    deadbolt.executor
    deadbolt.broker
    deadbolt.plan
    deadbolt.engine
    deadbolt.contracts

